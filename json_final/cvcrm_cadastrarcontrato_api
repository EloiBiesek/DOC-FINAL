{
  "method": "POST",
  "baseUrl": "https://integracao.cvcrm.com.br",
  "path": "/api/v1/comercial/reservas/contratos",
  "parameters": {
    "headers": {
      "email": { "type": "string", "description": "Email do usuário", "required": true },
      "token": { "type": "string", "description": "Token de autenticação", "required": true }
    },
    "body": {
      "idReserva": { "type": "integer", "description": "ID da reserva para vincular o contrato", "required": true },
      "arquivoContrato": { "type": "string", "description": "Arquivo do contrato em base64", "required": true }
    }
  },
  "examples": {
    "curl_with_body": "curl --request POST \\n+--url https://integracao.cvcrm.com.br/api/v1/comercial/reservas/contratos \\n+--header 'accept: application/json' \\n+--header 'content-type: application/json' \\n+--data '{\"idReserva\":123,\"arquivoContrato\":\"BASE64\"}'"
  },
  "responses": {
    "201": { "description": "Contrato cadastrado" },
    "400": { "description": "Requisição inválida" }
  }
}


