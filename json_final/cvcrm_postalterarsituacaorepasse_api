{
  "method": "POST",
  "baseUrl": "https://integracao.cvcrm.com.br",
  "path": "/api/v1/financeiro/repasses/{idRepasse}/alterar-situacao/{idSituacao}",
  "parameters": {
    "headers": {
      "email": { "type": "string", "description": "Email do usuário", "required": true },
      "token": { "type": "string", "description": "Token de autenticação", "required": true }
    },
    "path": {
      "idRepasse": { "type": "integer", "description": "ID do repasse", "required": true },
      "idSituacao": { "type": "integer", "description": "ID da nova situação", "required": true }
    }
  },
  "examples": {
    "curl": "curl --request POST \\n+--url https://integracao.cvcrm.com.br/api/v1/financeiro/repasses/123/alterar-situacao/2 \\n+--header 'accept: application/json'"
  },
  "responses": {
    "200": { "description": "Situação alterada com sucesso" },
    "400": { "description": "Requisição inválida" }
  }
}


