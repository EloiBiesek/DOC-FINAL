{
  "method": "GET",
  "baseUrl": "https://{dominiodocliente}.cvcrm.com.br",
  "path": "/api/v1/cliente/configuracoes",
  "parameters": {
    "headers": {
      "accept": { "type": "string", "description": "application/json" },
      "authorization": { "type": "string", "description": "Bearer JWT token", "required": true }
    }
  },
  "responses": {
    "200": { "description": "Configuração ativa do painel do cliente", "body": { "type": "object" } },
    "404": { "description": "Configuração não encontrada" }
  }
}

{
  "method": "GET",
  "baseUrl": "https://integracao.cvcrm.com.br",
  "path": "/api/v1/cliente/configuracoes",
  "parameters": {
    "headers": { "accept": { "type": "string", "description": "application/json" } }
  },
  "examples": { "curl": "curl --request GET \\\n+--url https://integracao.cvcrm.com.br/api/v1/cliente/configuracoes \\\n+--header 'accept: application/json'" },
  "responses": { "200": { "description": "Configurações do portal", "body": { "type": "object" } } }
}


